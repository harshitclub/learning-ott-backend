config:
  target: "http://localhost:3002"
  phases:
    - duration: 60       # Run for 30 seconds
      arrivalRate: 1000    # 10 new users per second
scenarios:
  - name: "Health Check API"
    flow:
      - get:
          url: "/api/health"